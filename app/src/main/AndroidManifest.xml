<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project48">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">


        <!-- IntroActivity 设置为启动活动 -->
        <activity android:name=".IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 其他活动定义 -->
        <activity android:name=".Forum.ForumActivity">
            <!-- Add any intent filters if needed -->
        </activity>
        <activity android:name=".AddToiletActivity">
            <!-- Add any intent filters if needed -->
        </activity>
        <activity
            android:name=".UserCommentActivity">
        </activity>
        <activity android:name=".detailActivity">
            <!-- Add any intent filters if needed -->
        </activity>
        <activity android:name=".listActivity">
            <!-- Add any intent filters if needed -->
        </activity>
        <activity android:name=".MainpageActivity">
            <!-- Add any intent filters if needed -->
        </activity>
        <activity android:name=".LoginActivity">
            <!-- Add any intent filters if needed -->
        </activity>
        <activity android:name="MainActivity" />
        <activity android:name="SignupActivity" />
        <!-- 如有其他活动，继续在此处添加 -->
        <activity android:name=".MapActivity" />
        <activity android:name=".MapActivity2" />
        <activity android:name=".MapActivity3" />
        <activity android:name=".MapActivity4" />

        <activity android:name=".Forum.AddPostActivity" />
        <activity android:name=".Forum.PostAndCommentActivity" />


        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCqvGKiSjyGkfzEICenPbFsk4t5iq3XipI" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
